# File: frontend/src/types/ffmpeg.d.ts

- [ ] Read and understand this file

What it is:
- Type declarations for an ffmpeg module.

What it does (easy words):
- Tells TypeScript that `createFFmpeg` and `fetchFile` exist.

Practical example:
- Without this file, TypeScript would complain when importing ffmpeg.
