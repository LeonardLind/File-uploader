# File: frontend/src/utils/ffmpegLoader.ts

- [ ] Read and understand this file

What it is:
- Helper to load ffmpeg in the browser.

What it does (easy words):
- Dynamically imports the ffmpeg library.
- Creates an ffmpeg instance and loads it.

Practical example:
- When an AVI is dropped, this loads ffmpeg so it can convert to MP4.
